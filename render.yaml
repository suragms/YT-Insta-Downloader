services:
  # Backend Service (Docker) - Free Tier
  - type: web
    name: yt-downloader-backend
    env: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: PORT
        value: 5000
